services:
  bot:
    build: .
    image: ghcr.io/quantummechanicsenjoyer/kurisumakise-bot:latest
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DATABASE_URL=sqlite:////app/database.db
    volumes:
      - .:/app
    command: /usr/local/bin/makise-kurisu-bot
